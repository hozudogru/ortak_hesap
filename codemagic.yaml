workflows:
  ios-workflow:
    name: "iOS Build"

    integrations:
      app_store_connect: Codemagic

    environment:
      flutter: stable
      xcode: latest
      cocoapods: default

      ios_signing:
        certificates:
          - ortakhesap-cert
        provisioning_profiles:
          - ortakhesap-profile